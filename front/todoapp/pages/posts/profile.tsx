import React from 'react'
import Header from '../../components/Header'

type Props = {}

const profile = (props: Props) => {
  return (
    <Header mainContent={<div>profile</div>} flgOpen={undefined}    ></Header>
  )
}

export default profile