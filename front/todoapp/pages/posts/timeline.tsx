import React from 'react'
import Header from '../../components/Header'

type Props = {}

const timeline = (props: Props) => {
  return (
    <Header mainContent={<div>timeline</div>} flgOpen={undefined}    ></Header>
    
  )
}

export default timeline