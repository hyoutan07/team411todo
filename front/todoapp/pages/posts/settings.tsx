import React from 'react'
import Header from '../../components/Header'

type Props = {}

const settings = (props: Props) => {
  return (
    <Header mainContent={<div>settings</div>} flgOpen={undefined}    ></Header>
  )
}

export default settings