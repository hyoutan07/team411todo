import React from 'react'
import Header from '../../components/Header'

type Props = {}

const calendar = (props: Props) => {
  return (
    <Header mainContent={<div>calendar</div>} flgOpen={undefined}    ></Header>
  )
}

export default calendar